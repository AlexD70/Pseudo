add comments 
improve dtypes::String, use Literal<String> inside parser
write IntegerToString cast, write StringToInteger cast and other casts
implement casts inside pseinternal.cpp
build basic parser
build token wrapper and token types + errors + warnings
build context validator and dtypes + errors + warnings
